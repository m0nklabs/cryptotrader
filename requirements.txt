# Core
python-dotenv>=1.2.2
requests>=2.34.2

# Technical analysis
pandas>=3.0.3
numpy>=2.4.6

# Desktop notifications (optional, for signal alerts)
plyer>=2.1.0

# Optional (only needed if you configure DATABASE_URL for indicator weights/signals)
SQLAlchemy>=2.0.50
psycopg2-binary>=2.9.12
asyncpg>=0.31.0  # Async PostgreSQL driver for SQLAlchemy async sessions

# FastAPI (for read-only API)
fastapi>=0.137.0
uvicorn>=0.49.0,<0.50.0
httpx>=0.25.0  # Async HTTP client for notifications

# WebSocket support (for real-time market data)
websocket-client>=1.9.0
websockets>=16.0
